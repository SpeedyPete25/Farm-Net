classDiagram
    class User {
        +int id
        +string email
        +string passwordHash
        +string role
        +register(email, password)
        +login(email, password)
        +logout()
        +changePassword(current, newPass)
    }


    class Equipment {
        +int id
        +string name
        +int quantity
        +getAvailability()
    }

    class EquipmentUnit {
        +int id
        +string code
    }

    class Booking {
        +int id
        +string date
        +string startTime
        +float durationHours
        +string status
        +create(user, roomId, date, startTime, duration)
        +cancel()
        +edit(date, startTime, duration)
    }

    class Loan {
        +int id
        +string borrowDate
        +string returnDate
        +string status
        +create(user, equipmentId, days)
        +cancel()
        +edit(returnDate)
    }

    class Room {
        +int id
        +string name
        +string location
        +getSchedule(date)
    }
    
    User "1" --> "0..*" Booking : makes
    User "1" --> "0..*" Loan : takes
    Room "1" --> "0..*" Booking : hosts
    Equipment "1" --> "1..*" EquipmentUnit : has units
    Equipment "1" --> "0..*" Loan : tracked by
    EquipmentUnit "1" --> "0..1" Loan : assigned to