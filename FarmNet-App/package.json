{
  "name": "lab-booking-system",
  "version": "1.0.0",
  "description": "University room booking and equipment checkout prototype",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "npm run test:reset && node --test tests/api.integration.test.js",
    "test:api": "npm run test:reset && node --test tests/api.integration.test.js",
    "test:browser": "npm run test:reset && playwright test --config=playwright.config.js",
    "test:reset": "node scripts/reset-test-data.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "multer": "^2.1.1",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
